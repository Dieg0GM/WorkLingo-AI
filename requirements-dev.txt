# Development and test dependencies (not needed to run the app)
# Install with: pip install -r requirements-dev.txt

# HTTP client (used by FastAPI TestClient for API tests)
httpx>=0.27.0,<1

# Test framework
pytest>=8.0
pytest-asyncio>=0.24
